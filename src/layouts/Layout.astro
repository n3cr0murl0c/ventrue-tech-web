---
import '../styles/global.css';
import SEO from '../components/SEO.astro';
import CookieConsent from '../components/CookieConsent.astro';
import SciFiBackground from '../components/SciFiBackground.astro';
import { languages } from '../i18n/ui';

interface Props {
  title: string;
  description?: string;
  image?: string;
  lang?: keyof typeof languages;
  type?: 'website' | 'article';
  publishedTime?: string;
  modifiedTime?: string;
  author?: string;
  section?: string;
  tags?: string[];
  /** Render the schematic starfield + grid + scan + CRT background. Default true. */
  bgEffect?: boolean;
}

const {
  title,
  description = 'Ventrue Tech — Software, cloud y automatización como servicio.',
  image = '/favicon.svg',
  lang = 'es',
  type = 'website',
  publishedTime,
  modifiedTime,
  author,
  section,
  tags,
  bgEffect = true,
} = Astro.props;

const url = `https://ventrue.tech${Astro.url.pathname}`;
---

<!doctype html>
<html lang={lang}>
  <head>
    <SEO 
      title={title}
      description={description}
      image={image}
      url={url}
      type={type}
      publishedTime={publishedTime}
      modifiedTime={modifiedTime}
      author={author}
      section={section}
      tags={tags}
    />
    <script is:inline>
      // Initialize theme before page renders to prevent flash
      const theme = localStorage.getItem('theme') || 
        (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      document.documentElement.classList.toggle('dark', theme === 'dark');
    </script>
  </head>
  <body class="min-h-screen">
    {bgEffect && <SciFiBackground />}
    <div class="relative z-10">
      <slot />
    </div>
    <CookieConsent lang={lang} />
  </body>
</html>
